# Kaleidoscope 使用说明（接入手册）

> 本手册面向**要拿 Kaleidoscope 做新界面/新客户项目的开发者**，从零开始讲清楚：
> 怎么引用、怎么配、怎么接、业务层怎么写、怎么热更、常见坑在哪。
> 想看"目录结构 + 四步速查"先读 `README.md`；本文是完整版。

---

## 目录

1. [一、能干什么 / 不能干什么](#一能干什么--不能干什么)
2. [二、引用方式与工程配置](#二引用方式与工程配置)
3. [三、整体架构与依赖关系](#三整体架构与依赖关系)
4. [四、配置文件逐项说明](#四配置文件逐项说明)
5. [五、四步接入（含完整代码骨架）](#五四步接入含完整代码骨架)
6. [六、业务层（协调器）怎么写](#六业务层协调器怎么写)
7. [七、热更：改配置免重启](#七热更改配置免重启)
8. [八、事件清单与线程模型](#八事件清单与线程模型)
9. [九、日志出口配置](#九日志出口配置)
10. [十、常见问题排查（FAQ）](#十常见问题排查faq)
11. [十一、红线与约束汇总](#十一红线与约束汇总)

---

## 一、能干什么 / 不能干什么

### 能干什么（库内已封装且经过现场验证）

| 设备 | 库内服务 | 能力 |
| --- | --- | --- |
| 汇川 PLC（Modbus TCP，两模式） | `PlcService`（从站）/ `ModbusTcpMasterClient`（主站） | **从站模式（默认）**：上位机监听 502，处理 PLC 主站的"请求-结果-复位"三拍握手，读写寄存器、上电结果复位、型号区写入；**主站模式**（`PlcRole=Master`）：上位机主动连 PLC 读写寄存器 + 自动轮询（`PlcMasterConfig.PollItems`） |
| 基恩士 IV4 相机 | `KeyenceIV4Camera` | TCP 无协议触发（T1/T2）、读判定（RT）、切程序（PW）、设输出格式（OF）、读图（BR）；FTP 推图监听与归档在 `ImageStore` |
| 基恩士 SR 扫码枪 | `ScannerTcpService` / `ScannerService` | TCP 无协议（连上自动发 `LON` 触发读码）与串口两种实现，统一 `IScanner` 接口；断线自动重连；串口模式支持 WMI 按关键词自动识别 |
| 气压表（Modbus RTU 主站） | `IBarometerReader` / `ModbusRtuBarometerReader` | 72 台真空负压表经 RS485→USB(CH340) 自动识别串口；读压力（kPa）、写报警阈值；`BarometerPort.cache` 记忆端口 |
| IO 耦合器（Modbus TCP 主站） | `IIoController` / `ModbusTcpIoController` | 读 DI/写 DO 控制真空电磁阀/载台上电；16 点/寄存器读-改-写；备用通道映射 |
| 冷却送风机（Modbus TCP） | `IFanController` / `FanControllerClient` | 定值启动/停止、读温度湿度（值 /100）；IP 记忆 `FanLastIp.cache` + 候选自动探测 |
| 图片存储 | `ImageStore` | FTP 推图监听（FileSystemWatcher 信号加速）、jpeg+iv4p 双格式归档、存图定期清理 |
| 连接健康 | `ConnectionMonitor` | 后台心跳、断连自动重连、边沿日志（连上/断开各提示一次），覆盖全部七类设备 |

### 不能干什么（留给业务项目自己写）

- **业务流程编排**（什么时刻触发扫码、什么时候拍哪个点位、结果怎么流转）——这是每个客户项目
  的业务核心，Kaleidoscope 只提供"设备怎么通讯"，不提供"生产怎么编排"。
- **界面 UI**（窗口矩阵、指示灯、设置窗体）。
- **业务项目的自有配置**（窗口布局/计数/安全等非设备字段）——设备层配置（`DeviceHubConfig`）
  **读写已收进库内**：`Configuration/ConfigSerializer`（零第三方依赖）一行存/读，保存前可用
  `Configuration/DeviceHubConfigValidator` 校验；业务项目不用再自己写 JSON 反序列化。

> 一句话定位：**Kaleidoscope 负责"设备活着 + 按指令干活"，业务项目负责"什么时候让设备干活 + 把结果展示出来"。**

---

## 二、引用方式与工程配置

### 2.1 引用 DLL

直接把 `Kaleidoscope/bin/Debug/Kaleidoscope.dll`（连同 `libs/NModbus.dll`）拷进业务项目，
在 csproj 里加引用：

```xml
<Reference Include="Kaleidoscope">
  <HintPath>..\Kaleidoscope\bin\Debug\Kaleidoscope.dll</HintPath>
</Reference>
<Reference Include="NModbus">
  <HintPath>..\Kaleidoscope\libs\NModbus.dll</HintPath>
</Reference>
```

> 若用到 `Kaleidoscope.Configuration`（`ConfigSerializer`/`DeviceHubConfigValidator`），其依赖
> 的 `System.Runtime.Serialization` / `System.Xml` 是 .NET Framework 自带，无需额外引用。

或者在 VS 里"添加引用 → 浏览"选中 `Kaleidoscope.dll` 即可（NModbus.dll 会被自动带上）。

### 2.2 目标框架

业务项目必须是 **.NET Framework 4.7.2 及以上**（`Kaleidoscope` 编译目标 4.7.2）。
`Kaleidoscope` 用 C# 7.3 特性（元组等），**不要降级**业务项目的 LangVersion。

### 2.3 离线可编译

`Kaleidoscope` 不依赖 NuGet restore，第三方 DLL（NModbus）就拷在 `libs/` 目录本地引用。
新增第三方库请遵循同一策略：**拷 dll 进 libs 再引用**。

---

## 三、整体架构与依赖关系

```
业务界面（新项目，只写 UI 和业务编排）
   │  只依赖：DeviceHub + 各服务实例 + 强类型配置
   ▼
DeviceHub（门面：建 / 启 / 事件聚合 / 热更 / 释放 全链路编排）
   │  内部创建并持有全部服务实例
   ▼
PlcService / ModbusTcpMasterClient / KeyenceIV4Camera / IScanner(串口+TCP) / ImageStore / ConnectionMonitor
IBarometerReader(气压表 Modbus RTU) / IIoController(IO 耦合器 Modbus TCP) / IFanController(送风机 Modbus TCP)
```

- **DeviceHub 是唯一门面**：业务项目不要直接 `new` 底层服务（`PlcService`/`ScannerService`…），
  全部通过 `DeviceHub` 拿实例。这样"建哪些服务、按什么顺序释放、事件怎么聚合"由库统一保证。
- **PLC 两模式**：`PlcRole=Slave`（默认）时 `hub.Plc` 是从站 `PlcService`；`PlcRole=Master` 时
  `hub.PlcMaster` 是主站 `ModbusTcpMasterClient`，另一者恒为 null——业务层用 `hub.IsPlcMaster` 判断。
- **业务层只能调用服务公开方法 + 订阅事件**，禁止自己新建 TcpClient/串口连接。
- **所有服务惰性连接**：`new DeviceHub(config)` 只建对象不连网络；真正第一次读写时才 `EnsureConnected`
  建连。连不上自动重试，恢复后自动连回。

---

## 四、配置文件逐项说明

配置全部是**强类型对象**，字段含义和默认值都在代码注释里。下面是接入时最常改的几项。

> **不想手写/拼这些配置？** 用独立工具 `ConfigEditor/`（仓库 `ConfigEditor/` 目录）可视化编辑：
> 左侧设备树选设备 → 右侧属性网格改参数（中文名/说明来自模型元数据）→ 底部按设备选**品牌预设**
> 一键填充（基恩士 IV4 / Honeywell Xenon 1902 / 三菱 GX-CL140 等）→ 保存前自动校验（错误阻止、警告确认）
> → 落盘 `.kcfg`。产出文件与下文手写/代码生成完全等价，接法同 V1.2.4：
> `var cfg = ConfigSerializer.Load(path); hub.ApplyConfig(cfg);`

### 4.1 `DeviceHubConfig`（总配置，DeviceHub 的入参）

```csharp
var config = new DeviceHubConfig
{
    ProductModel = "U171",              // 当前产品型号（PLC 建站成功立即写型号区）
    ProductModels = new List<string> { "U171", "Z121" }, // 型号候选列表
    PlcRole = PlcRole.Slave,            // PLC 通讯角色：Slave=上位机作从站（默认）/ Master=上位机作主站
    Plc = new PlcConfig { ... },        // PLC 从站配置（仅 PlcRole=Slave 生效）
    PlcMaster = new PlcMasterConfig { ... }, // PLC 主站配置（仅 PlcRole=Master 生效）
    Cameras = new List<CameraConfig> { ... },  // 相机配置（空列表兜底用 DefaultCameras）
    Scanners = new List<ScanConfig> { ... },   // 扫码枪配置（空列表则不留扫码枪）
    Image = new ImageConfig { ... },    // 图片存储配置
    Barometer = new BarometerConfig { ... }, // 气压表配置（Modbus RTU）
    Io = new IoConfig { ... },          // IO 耦合器配置（Modbus TCP）
    Fan = new FanConfig { ... },        // 送风机配置（Modbus TCP）
    UseMockCommunication = false,       // true=气压表/IO/送风机用 Mock，不接设备跑通 UI
};
```

### 4.2 `PlcMasterConfig`（PLC 主站，`PlcRole=Master` 时用）

上位机作 **Modbus TCP 主站**主动读写 PLC 时用（业务层通过 `hub.PlcMaster` 访问，
`DeviceHub.Start()` 自动启动后台轮询）。**寄存器地址一律填 Modbus 协议地址（0x0000 起：
协议 40001 = 地址 0，40002 = 地址 1），不要混用从站的 DataStore 索引约定。**

| 字段 | 默认 | 说明 |
| --- | --- | --- |
| `IpAddress` | `192.168.1.30` | 目标 PLC IP |
| `Port` | `502` | 目标端口（Modbus 标准 502） |
| `UnitId` | `1` | Modbus 从站地址 |
| `TimeoutMs` | `3000` | 连接/读写超时（TCP 连接用 BeginConnect 手动超时，IP 错不卡界面） |
| `ReconnectIntervalMs` | `10000` | 断线自动重连的最小间隔（节流） |
| `PollIntervalMs` | `0` | 自动轮询周期；0=不自动轮询（业务层按需读写） |
| `PollItems` | 空 | 轮询项列表（每条：名称 + 功能码 + 起始地址 + 长度），`StartPolling` 后后台定时读，结果走 `PollDataUpdated` 事件 / `GetLastPollData(name)` 缓存 |

> 主站模式的读写 API 都在 `ModbusTcpMasterClient`：读 `ReadHoldingRegisters` / `ReadInputRegisters` /
> `ReadCoils` / `ReadDiscreteInputs`；写 `WriteSingleRegister` / `WriteMultipleRegisters` /
> `WriteSingleCoil` / `WriteMultipleCoils`。连接状态边沿（`ConnectionChanged`）会聚合到
> `HubDeviceKind.Plc` 灯位。

### 4.3 `PlcConfig`（PLC 从站，`PlcRole=Slave` 时用）

| 字段 | 默认 | 说明 |
| --- | --- | --- |
| `IpAddress` | `0.0.0.0` | 从站监听 IP（本机）。PLC 主站要连到上位机的这个 IP |
| `Port` | `502` | 从站监听端口（Modbus 标准 502） |
| `ScanRequestAddress` | `1` | 扫码请求 DataStore 索引（协议 40001） |
| `ScanResultAddress` | `4` | 扫码结果 DataStore 索引（协议 40004） |
| `ProductModelIndexAddress` | `7` | 型号序号 DataStore 索引（协议 40007） |
| `ProductModelAddress` | `8` | 型号字符串起始 DataStore 索引（协议 40008） |
| `ProductModelLen` | `5` | 型号字符串占用的寄存器数（每寄存器 2 字符 ASCII，10 字符） |
| `ModelIndexes` | 空 | 型号名 → 序号映射（如 `{"U171":2,"Z121":1}`） |

> ⚠️ **地址一律存 DataStore 索引**，不是协议号！协议 40002 → 索引填 2，就是汇川 D2/D3 这类数字。
> 全链路零换算，别再做"协议号-40000"的中间换算（V2.12.3 已删掉这个坑）。

### 4.4 `CameraConfig`（每台相机）

| 字段 | 默认 | 说明 |
| --- | --- | --- |
| `CameraId` | `0` | 相机真编号（现场惯例 = 存图目录号）。纯标识，不参与通讯逻辑 |
| `Name` | `""` | 相机名（如 "上相机"），日志/界面显示用 |
| `IpAddress` | `192.168.0.10` | 相机 IP |
| `CommandPort` | `8500` | 控制指令端口（基恩士无协议常用 8500） |
| `FtpUploadDir` | `""` | 相机 FTP 推图目录（留空回退全局 FtpRootDir）。**多相机必须各自不同目录**，否则图会混 |
| `PlcRequestAddress` | `0` | 本相机在 PLC 从站的"拍照请求"索引（协议 40002=2）。**0=不参与 PLC 轮询** |
| `PlcResultAddress` | `0` | 本相机"拍照结果"索引（协议 40005=5）。0=不写结果 |
| `TriggerCommand` / `TriggerAndReadCommand` / `ReadResultCommand` | `T1` / `T2` / `RT` | IV4 指令名（一般不用改） |
| `OutputFormat` | `""` | 判定输出格式 `OF,nn`（"00"~"03"，**必须恰好 2 位数字**，留空不发） |
| `ReadResultFromCamera` | `true` | true=用相机判定；false=退化"图到即记 OK" |
| `OkChar` | `"0"` | 判定合格字符（IV4：'0'=OK、'1'=NG，遇 '4'/'-' 保守判 NG） |
| `ResponseTimeoutMs` | `5000` | 等待 T2/RT 拍摄判定响应毫秒数 |
| `TimeoutMs` | `3000` | 单次收发包超时毫秒数 |
| `ImageWaitMs` | `10000` | 触发后等 FTP 新图的最长毫秒数 |
| `StationPrograms` | 空 | "点位→程序号"默认映射表（型号没配表时回退） |
| `ModelStationPrograms` | 空 | 按型号分的"点位→程序号"映射表（运行时优先查本表） |

**点位→程序号映射**（现场核心配置）：

```csharp
new CameraConfig
{
    Name = "上相机",
    CameraId = 2,
    IpAddress = "19.87.6.213",
    FtpUploadDir = @"D:\IV存图\2",
    PlcRequestAddress = 2,   // 协议 40002
    PlcResultAddress = 5,    // 协议 40005
    ModelStationPrograms = new List<ModelStationPrograms>
    {
        new ModelStationPrograms
        {
            ModelName = "U171",
            Programs = new List<StationProgramItem>
            {
                new StationProgramItem { StationNo = 1, ProgramNo = 0 },
                new StationProgramItem { StationNo = 2, ProgramNo = 1 },
                // ... 该型号下本相机负责的点位 → 相机程序号
            }
        }
    }
};
```

> 触发切程序的语义：本相机在拍"点位 N"前，先发 `PW,xxx` 切到对应程序号，命中才切、未命中不切。
> **程序号 0 也是合法程序**（P000），别把 0 当"未设置"。

### 4.5 `ScanConfig`（每台扫码枪）

| 字段 | 默认 | 说明 |
| --- | --- | --- |
| `Enabled` | `false` | 是否启用 |
| `Mode` | `"Tcp"` | `"Tcp"`=以太网无协议（默认，现场实测）；`"Serial"`=串口 |
| `IpAddress` | `19.87.6.100` | TCP 模式扫码枪 IP |
| `Port` | `9004` | TCP 模式端口（基恩士 SR 无协议默认） |
| `TriggerCommand` | `"LON"` | TCP 模式连接成功后的触发指令（打开激光/开始读取），自动补 `\r\n` |
| `PortName` | `""` | 串口模式串口名；**留空自动识别**（按 `DeviceKeyword` 用 WMI 查设备名定位串口，现场免配） |
| `BaudRate` | `115200` | 串口模式波特率 |
| `DataBits` | `8` | 串口模式数据位 |
| `StopBits` | `"1"` | 停止位字符串：`"1"/"15"/"2"` |
| `Parity` | `"None"` | 校验位枚举名：`None/Odd/Even/Mark/Space` |
| `DeviceKeyword` | `"Xenon 1902"` | 串口自动识别关键词（设备管理器里的设备名关键词，如霍尼韦尔 Xenon 1902）；`PortName` 留空且 `Mode=Serial` 时用 |
| `DebugLog` | `false` | 串口心跳调试日志开关（现场排查"断连识别不到"时开） |

> ⚠️ **TCP 扫码枪不是连上就回数据**！必须发触发指令（`LON`）才开始读码。本库在每次连接/重连
> 成功后自动发一次，配置留空则不发送。

### 4.6 `ImageConfig`（图片存储）

| 字段 | 默认 | 说明 |
| --- | --- | --- |
| `SaveRootDir` | `D:\Images` | 存图根目录 |
| `SubDirs` | 含 `{年月日}/{SN}/{相机}/{OKNG}` | 归档子目录结构。**每项只能是一层目录名/规则**（`{SN}`、`{相机}`、`OK`…），禁止把整个绝对路径粘成一个层级（会叠出超长嵌套目录，库内已有三层防御会自愈） |
| `FileNameTemplate` | `{点位}` | 文件名模板（旧版兼容；**V2.14.11 起归档文件名 = 相机源文件名 + "_" + 时间戳**） |
| `FileTimestampSuffix` | `true` | 是否追加时间戳后缀 |
| `KeepDays` | `30` | 存图保留天数（0=不自动清理） |
| `FtpRootDir` | `D:\IV存图` | 相机 FTP 推图根目录（相机 `FtpUploadDir` 留空时回退） |

### 4.7 `BarometerConfig`（气压表，Modbus RTU 主站）

> 现场：72 台真空负压表挂在 RS485 总线，经 USB 转串口（CH340）接入工控机。上位机作 **Modbus RTU 主站**逐台读压力/写阈值。

| 字段 | 默认 | 说明 |
| --- | --- | --- |
| `TotalBarometers` | `72` | 气压表台数（= 从站 slaveId 范围，第 i 台 slaveId=i） |
| `PortName` | `""` | 串口名；**留空自动识别 CH340**（WMI 双重校验 VID_1A86/PID_7523），命中记忆到 `BarometerPort.cache` |
| `BaudRate` / `DataBits` / `StopBits` / `Parity` | `19200` / `8` / `"1"` / `"None"` | 串口参数（RS485→USB，现场实测 19200 8N1） |
| `SerialReadTimeoutMs` / `SerialWriteTimeoutMs` | `1000` | 串口读写超时（单台无响应时快速跳过，不拖慢整线轮询） |
| `BarometerPressureRegisterAddress` | `0x0001` | 压力保持寄存器（读 0x04，2 寄存器） |
| `BarometerDefaultDecimalPlaces` | `1` | 小数位（**固定用配置，0x0002 不可靠**） |
| `BarometerPressureScale` | `1` | 压力缩放系数（原始值 × 系数 = kPa） |
| `AlarmPressureThresholdKPa` | `-95` | 写回每台设备的报警阈值（0x06 @ 0x0010） |
| `AlarmWhenPressureHigherThanThreshold` | `true` | 报警方向：压力 > 阈值报警（负压泄漏判高） |

**业务层用法**（建议后台定时轮询，**勿放 UI 线程**）：

```csharp
BarometerData[] all = hub.Barometer.ReadAllData();   // 未连接返回全 null 数组（UI 照常显示"未连接"）
if (all[5] != null) { double kpa = all[5].VacuumPressure; int st = (int)all[5].Status; }
hub.Barometer.SetAllThresholds(-95);                  // 批量写阈值（内部 50ms 间隔逐台写，勿手动循环）
```

### 4.8 `IoConfig`（IO 耦合器，Modbus TCP 主站）

> 现场：IO 耦合器挂 PLC 侧工业交换机，上位机作 Modbus TCP 主站读 DI/写 DO，控制真空电磁阀/载台上电。

| 字段 | 默认 | 说明 |
| --- | --- | --- |
| `TotalInputs` / `TotalOutputs` | `80` / `160` | 输入/输出总点数（内部编号：输入 1~80，输出 81~240） |
| `PlcAddress` / `PlcPort` | `192.168.1.20` / `502` | IO 耦合器 IP 与端口 |
| `IoUnitId` | `1` | Modbus 从站地址 |
| `IoInputRegisterStartAddress` | `0x1000` | 输入区起始寄存器（读 0x04，**16 点/寄存器**） |
| `IoOutputRegisterStartAddress` | `0x2000` | 输出区起始寄存器（写 0x03/0x06） |
| `InvertInputs` / `InvertOutputs` | `false` | 输入/输出电平取反（现场接线 NPN/PNP 不符时用） |
| `TcpSendTimeoutMs` / `TcpReceiveTimeoutMs` | `3000` | TCP 读写超时 |
| `IoBackupChannelMappingEnabled` | `false` | 启用备用通道映射（业务输出号 → 物理输出号） |
| `IoBackupChannelMappings` | 空 | 映射表（`ParseAll` 支持 `5->17` 或 `0x05->0x11`，通道 0~31） |

**业务层用法**：

```csharp
IoStatus[] inputs = hub.Io.ReadAllInputs();     // 读全部输入（真值/电平）
hub.Io.WriteOutput(3, true);                    // 单点写输出（内部"读-改-写"，不会覆盖同寄存器其它点）
bool cur = hub.Io.ReadOutput(3);                // 回读
```

> ⚠️ **单点写输出必须走"读-改-写"**：DO 区 16 点/寄存器，直接写单点会覆盖同寄存器其它输出。库内 `WriteOutput` 已内置，**业务层禁止自己拼 Modbus 写单寄存器覆盖**。

### 4.9 `FanConfig`（冷却送风机，Modbus TCP）

> 现场：送风机控制屏（Modbus TCP，端口 50000 不是 502）。上位机作主站定值启动/停止 + 读温湿度。

| 字段 | 默认 | 说明 |
| --- | --- | --- |
| `FanEnabled` | `true` | 是否启用送风机服务 |
| `FanIpAddress` | `192.168.1.220` | 送风机 IP |
| `FanPort` | `50000` | 端口（**不是 502**） |
| `FanUnitId` | `1` | Modbus 从站地址 |
| `FanTimeoutMs` | `3000` | 读写超时 |
| `FanAutoDetectEnabled` | `true` | 自动探测：按 `FanIpCandidates` 逐个试连找真机 |
| `FanIpCandidates` | 空 | 候选 IP 列表（IP 变了自动换候选试连） |

寄存器（值 **÷100** 才是真实值）：
- `0x0001` 控制/状态字：`0x0003`=定值启动、`0x0002`=定值停止
- `0x0002` 温度 / `0x0003` 湿度 / `0x0004` 温度设定 / `0x0005` 湿度设定

**业务层用法**：

```csharp
var st = hub.Fan.ReadStatus();          // 温湿度 + 运行状态（÷100 已完成）
hub.Fan.StartFixedValue();              // 定值启动（或 hub.Fan.Stop() 停止）
string ip = hub.Fan.ActiveIp;           // 当前实际连上的 IP
```

### 4.10 `UseMockCommunication`（模拟通讯开关）

`DeviceHubConfig.UseMockCommunication = true` 时，**气压表/IO 耦合器/送风机三个服务自动换成
Mock 实现**（随机数模拟数据、输出翻转模拟），不接任何线也能把 UI 和业务流程完整跑通；
接真机改回 `false` 即可，业务代码一行不用动。扫码枪/PLC/相机不受此开关影响（各自按配置真实连接）。

---

## 五、四步接入（含完整代码骨架）

### 第 1 步：配置

构造 `DeviceHubConfig`（推荐 `ConfigSerializer.Load` 读 `.kcfg` 文件，缺字段自动补默认值；
也可以直接代码构建）。

### 第 2 步：创建并启动

```csharp
// 建议在窗体/程序启动处：
var hub = new DeviceHub(config);   // 只建对象，不连网络（惰性）
hub.Start();                       // 扫码枪连接 + 心跳 + 存图清理

// 订阅聚合事件（事件在工作线程触发！UI 里必须 Invoke 回 UI 线程）
hub.SerialNumberScanned += (s, code) => BeginInvoke(new Action(() => lblSerial.Text = code));
hub.DeviceConnectionChanged += (s, e) => BeginInvoke(new Action(() => UpdateLamp(e.Kind, e.Name, e.Connected)));
```

### 第 3 步：业务层使用服务实例

把 `hub.Plc`（从站）**或** `hub.PlcMaster`（主站，按 `hub.IsPlcMaster` 取）+ `hub.Cameras` /
`hub.Scanners` / `hub.ImageStore` / `hub.Barometer` / `hub.Io` / `hub.Fan` 传给业务协调器。

### 第 4 步：关窗释放

```csharp
private void MainForm_FormClosing(object sender, FormClosingEventArgs e)
{
    hub.Dispose();   // 顺序：监控→PLC→气压表/IO/送风机→扫码枪→相机→图像存储，各步异常不中断
}
```

### 完整骨架

```csharp
public partial class MainForm : Form
{
    private DeviceHub _hub;
    private ProductionCoordinator _coordinator;  // 你自己的业务协调器

    public MainForm()
    {
        InitializeComponent();
        Init();   // 见下
    }

    private void Init()
    {
        // 1) 建配置（推荐：ConfigSerializer 一行读回强类型；也可代码构建）
        var config = ConfigSerializer.Load(@"Config\devices.kcfg");
        //    保存前可校验：var r = DeviceHubConfigValidator.Validate(config);  // r.IsValid 才允许保存

        // 2) 创建设备层并启动
        _hub = new DeviceHub(config);
        _hub.Start();

        // 3) 订阅聚合事件（注意线程）
        _hub.SerialNumberScanned += OnAnyScannerScanned;
        _hub.DeviceConnectionChanged += OnAnyDeviceConnectionChanged;
        _hub.ServicesRebuilt += OnServicesRebuilt;   // 热更后重建业务层

        // 4) 建业务协调器（你的编排逻辑，吃服务实例）
        //    PLC 取法：hub.IsPlcMaster ? (object)hub.PlcMaster : hub.Plc，主站模式 hub.Plc 为 null
        _coordinator = new ProductionCoordinator(_hub.Plc, _hub.Cameras, _hub.Scanners, _hub.ImageStore,
            _hub.Barometer, _hub.Io, _hub.Fan);
        _coordinator.Start();
    }

    private void OnServicesRebuilt(object sender, EventArgs e)
    {
        // 热更后：旧协调器握着已释放的服务引用，必须换新
        _coordinator?.Dispose();
        _coordinator = new ProductionCoordinator(_hub.Plc, _hub.Cameras, _hub.Scanners, _hub.ImageStore,
            _hub.Barometer, _hub.Io, _hub.Fan);
        _coordinator.Start();
    }

    protected override void OnFormClosing(FormClosingEventArgs e)
    {
        _coordinator?.Dispose();
        _hub.Dispose();
        base.OnFormClosing(e);
    }
}
```

---

## 六、业务层（协调器）怎么写

Kaleidoscope 不提供业务编排，但把设备通讯的手艺全部封装好了。业务层只需按下面三个"成品流程"
拼装即可——这些流程的每一步设备通讯细节（超时/重连/判定解析/归档）都在库内，业务层只做**决策**。

### 6.1 PLC 三拍握手（从站模式，V2.7 定稿协议）

现场 PLC（汇川）作主站，上位机作从站监听本机 502。**请求-结果-复位**三拍：

```
PLC 写请求≠0 → 上位机处理完写结果≠0 → PLC 读走并复位请求=0 → 上位机看请求清零再复位结果=0 → 进入下一拍
```

寄存器（DataStore 索引）：
- 40001=扫码请求；40002=第1台相机请求（1~255=点位）；40003=第2台相机请求
- 40004=扫码结果（0/1/2）；40005/40006=相机结果（0/1/2，相机另支持 3=点位禁用跳过）
- 40007=型号序号；40008~40012=型号 ASCII 字符串

业务层轮询模式（放在后台 `System.Threading.Timer`，**绝不放 UI 线程**）：

```csharp
// 每 200ms 轮询：
if (_plc.ReadScanRequest(out bool scanReq) && scanReq)   // 扫码请求到位
{
    // 新一轮开始 → 通知 UI 清空窗口图片 → 触发扫码枪 / 等扫码结果
    // 处理完写扫码结果：
    _plc.WriteScanResult(1);   // 0=无/1=OK/2=NG
    // 等 PLC 复位请求后：
    _plc.WriteScanResult(0);   // 复位结果，进入下一拍
}
```

> 完整的 PLC 协议见 CommandCenter 项目 `docs/CommandCenter.md` §5.5（本库抽取自该项目，协议一致）。

### 6.1b PLC 主站模式（`PlcRole=Master`，V1.2.0 新增）

上位机作 Modbus TCP **主站**主动读写 PLC 时，业务层用 `hub.PlcMaster`（`ModbusTcpMasterClient`）。
连接由监控心跳节流自愈，`DeviceHub.Start()` 已自动启动后台轮询，业务层直接读写即可：

```csharp
// 读保持寄存器（功能码 0x03，地址按 Modbus 协议地址 0x0000 起）：
ushort[] vals = _plcMaster.ReadHoldingRegisters(0, 6);   // 协议 40001~40006
if (vals != null && vals[0] != 0) { /* 有请求，处理... */ }

// 写结果（功能码 0x06）：
_plcMaster.WriteSingleRegister(3, 1);                     // 写协议 40004

// 后台轮询数据（已由 StartPolling 自动读回，业务层取缓存即可）：
object cached = _plcMaster.GetLastPollData("pressure");   // Name=PlcMasterConfig.PollItems 里的轮询项名
```

注意：主站模式没有从站的"三拍握手"与 DataStore 索引约定，寄存器地址一律填 **Modbus 协议地址**，
读写成功与否看返回（读失败返回 null、写失败返回 false），别用从站的 `ReadScanRequest` 等三拍方法。

### 6.2 相机拍照流程（判定即写，V2.13.7 定稿）

```
收到相机请求（带点位号）→ 按点位查程序号 → 切程序（命中才切）→ T2 触发+判定
→ 判定一返回【立即】写 PLC 结果（1/2，不等图！）
→ FTP 取图 + 归档 + 显示 全异步（图缺失只记日志报警，结果不回退）
→ 等"PLC 已复位请求 且 拍照 Task 完全结束"才释放该相机通道（防下一拍并发混图）
```

```csharp
var outcome = cam.TriggerAndRead();   // T2：触发 + 读判定，一次完成
if (outcome.Succeeded)
{
    _plc.WriteCameraResult(camConfig, outcome.IsOk ? 1 : 2);  // 判定即写，不等图
    // 异步取图归档（后台 Task）：
    Task.Run(() => {
        var pair = imageStore.FindLatestPair(dir);   // 扫目录取最新 jpeg+iv4p 对
        if (pair != null)
        {
            imageStore.SaveImageFilePair(pair.JpegPath, pair.IvpPath, stationNo, outcome.IsOk, serial, cameraName);
            imageStore.DeleteSourceFile(pair.JpegPath, pair.IvpPath);  // 处理即删
        }
    });
}
```

### 6.3 扫码枪使用

```csharp
// 触发扫码（TCP 模式手动重发触发指令；串口为空操作）：
scanner.SendTrigger();

// 收条码（已由 DeviceHub 聚合，也可直接订阅每台）：
scanner.SerialNumberScanned += (s, code) => { /* code 就是条码 */ };
```

### 6.4 图片显示（后台解码 + 缩略图，铁律）

**禁止在 UI 线程"读盘 + GDI+ 解码 + 全尺寸赋值"**（基恩士原图 2592×1944，每刷必卡）。做法：

```csharp
// 后台线程解码 + 等比降采样到最大边 1280，再 BeginInvoke 回 UI 给小图
Task.Factory.StartNew(() =>
{
    var thumb = LoadThumbnailSafe(jpegPath);   // 参考 CommandCenter ProductionCoordinator.LoadThumbnailSafe
    BeginInvoke(new Action(() => { picBox.Image?.Dispose(); picBox.Image = thumb; }));
});
```

### 6.5 气压表 / IO 耦合器 / 送风机（Aging 型主站设备）

三类设备都是**主站模式**（上位机主动连设备），连接由 DeviceHub 启动的心跳监控在后台自动建立，
业务层只管定时采集与控制。均**放后台 Timer，勿放 UI 线程**：

```csharp
// 气压表：定时采集全部 72 台真空压力（未连接返回全 null，UI 显示"未连接"即可）
private void BaroTimer_Tick(object sender, EventArgs e)
{
    BarometerData[] all = hub.Barometer.ReadAllData();
    lblVacuum.Text = all[5] != null ? all[5].VacuumPressure.ToString("F1") + " kPa" : "未连接";
}

// IO 耦合器：读输入（真空电磁阀到位信号等）+ 控制输出
private void IoTimer_Tick(object sender, EventArgs e)
{
    IoStatus[] inputs = hub.Io.ReadAllInputs();     // 轮询读输入
    if (inputs[1]?.Value == true) hub.Io.WriteOutput(3, true);   // 某输入到位 → 开某输出
}

// 送风机：启动/停止 + 读温湿度
if (btnFanRun.Checked) hub.Fan.StartFixedValue(); else hub.Fan.Stop();
var fan = hub.Fan.ReadStatus();                     // fan.Temperature / fan.Humidity / fan.RunState
```

> 断线处理：三类服务接口只有 `IsConnected` 属性 + `OnError` 事件，连接状态边沿由
> `DeviceHub.DeviceConnectionChanged` 聚合事件统一上报（`Kind` = Barometer/Io/Fan），
> UI 刷新指示灯即可，重连由监控器后台自动完成。

---

## 七、热更：改配置免重启

```csharp
_hub.ApplyConfig(newConfig);
```

**内部自动完成**：
1. 按固定顺序释放旧服务：监控 → PLC → 气压表/IO 耦合器/送风机 → 扫码枪 → 相机 → **图像存储**
   （ImageStore 必须显式释放，否则 FileSystemWatcher 句柄泄漏）；
2. 用新配置全量重建服务；
3. 重新订阅聚合事件；
4. 触发 `ServicesRebuilt` 事件。

**你只需要做一件事**：在 `ServicesRebuilt` 回调里重建自己的业务协调器并重新订阅——
因为旧协调器还握着已 Dispose 的服务引用，继续用会崩。

```csharp
_hub.ServicesRebuilt += (s, e) =>
{
    _coordinator?.Dispose();   // 停旧编排（各服务已由 hub 释放）
    _coordinator = new ProductionCoordinator(_hub.Plc, _hub.Cameras, _hub.Scanners, _hub.ImageStore,
        _hub.Barometer, _hub.Io, _hub.Fan);
    _coordinator.Start();
};
```

> 热更是本库的**一等公民**：所有服务都做到 Dispose 干净（限时抢锁 + 锁外强断网）+ 惰性连接
> 自动重连。新增服务类必须遵守，否则热更会残留旧线程/旧连接。

---

## 八、事件清单与线程模型

### 8.1 DeviceHub 聚合事件（界面优先用这些）

| 事件 | 签名 | 含义 | 触发线程 |
| --- | --- | --- | --- |
| `SerialNumberScanned` | `EventHandler<string>` | 任意扫码枪读到条码（多枪聚合） | 扫码枪接收线程 |
| `DeviceConnectionChanged` | `EventHandler<HubConnectionChangedEventArgs>` | 任一设备连接状态变化（`Kind`=PLC/相机/扫码枪/气压表/IO 耦合器/送风机，`Name`=标识，`Connected`=是否连接） | 各服务后台线程 |
| `FtpFileArrived` | `Action<int,string>` | 任一相机 FTP 目录有新图到达（参数=相机下标, 路径） | watcher 线程 |
| `ServicesRebuilt` | `EventHandler` | 热更完成，设备层已换新 | 调用 ApplyConfig 的线程 |

### 8.2 各服务原始事件（业务层用）

| 服务 | 事件 | 含义 |
| --- | --- | --- |
| `PlcService` | `ConnectionChanged` | 从站监听就绪/失败 |
| `PlcService` | `MasterConnectionChanged` | PLC 主站连入/断开 |
| `ModbusTcpMasterClient` | `ConnectionChanged` | 主站连接/断开（边沿触发） |
| `ModbusTcpMasterClient` | `PollDataUpdated` | 后台轮询读回一段数据（`PlcPollEventArgs`） |
| `ModbusTcpMasterClient` | `OnError` | 通讯错误回调 |
| `KeyenceIV4Camera` | `ConnectionChanged` | 相机连接/断开（边沿触发） |
| `IScanner` | `SerialNumberScanned` | 收到条码 |
| `IScanner` | `ConnectionChanged` | 扫码枪连接/断开 |
| `ImageStore` | `FtpFileArrived` | FTP 目录新图到达 |
| `IBarometerReader` / `IIoController` / `IFanController` | `OnError` | 通讯错误回调（连接状态走 `DeviceHub.DeviceConnectionChanged`，见 §8.1） |

### 8.3 线程模型（重要）

- **服务层所有事件都在工作线程触发**，UI 订阅方必须用 `Invoke`/`BeginInvoke` 回 UI 线程再操作控件。
- **UI 线程禁做任何网络 IO**。所有连接/读写都在服务后台线程完成。
- **Connect 一律强制超时**（`BeginConnect + WaitOne`），对不可达 IP 不会冻结 UI。

---

## 九、日志出口配置

`LogHelper.LogAction` 是全局可替换出口，默认写 `Logs/运行日志_日期.log`（常驻流 + 跨天滚动，
UTF-8 无 BOM，`AutoFlush` 即时落盘）。业务项目启动时注入文件出口：

```csharp
LogHelper.LogAction = (level, msg) =>
{
    File.AppendAllText(LogPath,
        $"[{DateTime.Now:yyyy-MM-dd HH:mm:ss.fff}][{level}] {msg}\r\n", Encoding.UTF8);
};
```

> ⚠️ 文件日志用 UTF-8 编码，避免中文乱码；Logs/ 目录记得 gitignore。

---

## 十、常见问题排查（FAQ）

**Q1：PLC 主站明明通了，上位机从站就是收不到请求？**
- 先看是否漏了 NModbus 从站网络释放（热更/重建后 PLC 认为旧连接还活着）：`Dispose`/重建时必须
  `_network?.Dispose()`，只 `_listener.Stop()` 会残留旧 master socket → PLC 不重连新从站。库内已
  在三处清理点统一补全，别在业务层再手动 Stop。
- 检查请求地址是否填成协议号（应填 DataStore 索引，40002 → 填 2）。

**Q1b：主站模式（`PlcRole=Master`）下 PLC 连不上 / 读写失败？**
- 先确认 `DeviceHubConfig.PlcRole=Master` 且 `PlcMaster.IpAddress/Port` 正确，从站配置 `Plc` 不生效。
- 主站模式的**寄存器地址填 Modbus 协议地址**（0x0000 起），不是从站的 DataStore 索引——混用会读到错误区域。
- 连接状态看 `DeviceConnectionChanged` 的 `Kind=Plc`；读写失败看 `hub.PlcMaster.OnError` 的报错文本。
- 断线会自动重连（监控心跳 5s 节流 + 客户端内部 `ReconnectIntervalMs`），无需手动处理。

**Q1c：PLC 修改了轮询时间（改大/改小），上位机从站会不会出问题？**
- **都不会**。从站模式下上位机是被动响应方（Modbus 主从问答协议，从站不感知主站轮询节奏）：
  轮询改小握手更快、改大握手更慢，功能始终正确；每个握手值（请求/结果）都"保持到对端响应才复位"，
  不丢不冲。库内 `_lock` 串行化 DataStore 读写，快慢都无竞态；`MasterPollTick`（1s）只查 TCP
  会话列表、`TcpKeepAlive`（5s）只探活不按应用层数据频率判死，都不会被轮询快慢误伤。
- **改慢时唯一边界**：业务协调器"等 PLC 复位请求"的超时须大于 PLC 最慢轮询周期（例：PLC 5s 轮询
  → 超时配 8s），否则 PLC 慢到超过超时时协调器会提前复位结果、握手提前结束。
- **改快时唯一边界**：PLC 梯形图必须严格遵守三拍时序——**发下一拍请求前先确认上位机结果已回 0**
  （或至少先复位请求、等上位机看到请求回 0 再复位结果）。快轮询只是把不合规梯形图（上一拍结果未
  复位就发新请求 / 持续置位不复位）的混拍窗口暴露得更频繁，库本身无此问题。

**Q2：扫码枪 Test-NetConnection 通，但程序收不到码？**
- TCP 扫码枪连上后必须发触发指令（`LON`）才开始读码。确认 `ScanConfig.TriggerCommand` 不为空
  （默认 `"LON"`），且 `Mode` 是 `"Tcp"`（V2.13.8 起默认 TCP，别误配成 Serial）。

**Q3：相机触发对了，但窗口图/判定不对？**
- 判定以相机为准（判定即写，图缺失不影响结果）；图不对先查 `FtpUploadDir` 是否张冠李戴
  （上相机推到 `\2`、下相机推到 `\1`，别写反）。
- 点位→程序号映射：确认 `ModelStationPrograms` 里当前型号的表配了该点位。

**Q4：第一次触发正常、第二次判定失败？**
- 这是"CRLF 残留 + 假活连接复用"两个现场级 bug 的表现，库内已修复（行首跳行尾符 +
  超时一律 MarkDisconnected）。若复现，多半是旧版本 DLL，重新编译引用最新 Kaleidoscope。

**Q5：存图目录越来越深、越来越乱？**
- `SubDirs` 每项只能是一层名字（`{SN}`、`{相机}`…），别把整个绝对路径粘成一个层级。库内有三层
  防御会自动拆/拦截，但正确的配置写法能根除。

**Q6：改相机 IP 要改几处？**
- 一处：`CameraConfig` 配置（推荐业务项目在配置里维护），或库内 `CameraConfig.DefaultCameras()`
  兜底值。**不要**在业务代码里再写死 IP。

**Q7：界面点按钮半天才响应（疑似卡死）？**
- 大概率 UI 线程做了网络 IO。所有设备调用放后台线程/Task，Connect 必须超时。检查是否直接调了
  `TcpClient.Connect` 或同步 `ReadHoldingRegisters`。

**Q8：接真机后气压表/IO/送风机连不上，但 Demo 里好好的？**
- 先确认 `UseMockCommunication=false`（true 时根本不连真机）；再查 `DeviceConnectionChanged` 里
  对应设备的断开原因，或订阅服务 `OnError` 看通讯错误。气压表确认 CH340 串口已被系统识别
  （设备管理器 USB-SERIAL CH340）；IO/送风机用 `Test-NetConnection IP -Port 502/50000` 探通。

**Q9：气压表串口自动识别选错了口？**
- `PortName` 留空时按 CH340 VID/PID（VID_1A86/PID_7523）双重校验识别，命中后记忆到
  `BarometerPort.cache`。若插了多个 CH340 或换 USB 口，删掉程序目录下的 `BarometerPort.cache`
  重试即可。

---

## 十一、红线与约束汇总

1. **UI 线程禁做网络 IO**；TCP 连接强制超时（`BeginConnect + WaitOne`）。
2. **事件在工作线程触发**，UI 订阅必须 `Invoke` 回 UI 线程。
3. **图片显示后台解码 + 缩略图**，禁全尺寸大图直接赋值 PictureBox。
4. **服务必须支持热更**：Dispose 干净 + 惰性连接 + 状态无残留。
5. **业务层禁止新建连接**，只用服务公开方法/事件。
6. **ImageStore 归 DeviceHub 所有**，别在业务协调器里 Dispose 它。
7. **地址约定**：从站模式（`PlcRole=Slave`）填 **DataStore 索引**（协议号 = 索引 + 40000），零换算；主站模式（`PlcRole=Master`）填 **Modbus 协议地址**（0x0000 起）。两种模式不要混用地址约定。
8. **日志只记边沿**（连上/断开各一次），中间失败静默节流。
9. 配置文件 UTF-8，`Logs/`、`bin/`、`obj/` gitignore。
10. **注释详实是库的第一红线**：接入者靠注释当文档，写不清等于没写。
11. **单点写 IO 输出必须走库内"读-改-写"**（`hub.Io.WriteOutput`），禁止业务层拼 Modbus 写单寄存器覆盖同寄存器其它输出。
12. **气压表批量写阈值必须用 `SetAllThresholds`**（内部 50ms 间隔逐台写，串口共享会互相干扰），禁止业务层自己循环连写。

---

> 更多通讯细节（PLC 完整协议、相机指令帧、扫码枪对接）源自 CommandCenter 现场项目，
> 见其 `docs/CommandCenter.md`（第四部分相机 / 第五部分 PLC §5.5 / 第三部分扫码枪）。
